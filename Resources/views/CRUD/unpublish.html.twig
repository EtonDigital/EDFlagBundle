{% extends base_template %}

{% block content %}
    <div class="sonata">

        <div class="box box-primary">
            <div class="box-header">
                <h3 class="box-title">Unpublish action</h3>
            </div>

            <form method="POST" action="{{ admin.generateObjectUrl('unpublish', object) }}">
                <div class="box-body">
                    Add comment to your action

                    <div class="form-group">
                        <div class="sonata-ba-field">
                            {{ form_widget(form.comment, {'attr': {'class': 'form-control' }}) }}
                        </div>
                    </div>
                </div>

                <div class="box-footer clearfix">
                    <button type="submit" class="btn btn-success"><i class="fa fa-level-down" aria-hidden="true"></i> Unpublish</button>
                </div>
            </form>
        </div>
    </div>
{% endblock %}